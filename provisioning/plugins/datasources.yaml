apiVersion: 1

# Note: This file configures plugin datasource types, not actual datasource instances
# Actual datasource instances are configured in the datasources folder
datasources:
    - type: 'jaops-yamcs-datasource'
      orgId: 1
      orgName: 'JAOPS'
      disabled: false
