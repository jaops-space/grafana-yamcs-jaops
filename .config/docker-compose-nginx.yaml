# Override for nginx deployment (ip/grafana/)
services:
  grafana:
    environment:
      # Subpath configuration for reverse proxy
      GF_SERVER_DOMAIN: ""
      GF_SERVER_ROOT_URL: "%(protocol)s://%(domain)s/grafana"
      GF_SERVER_SERVE_FROM_SUB_PATH: "true"
